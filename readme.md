# JavaScript

- Common JavaScript
- Module JavaScript - also called as ES6 JavaScript

## Common JavaScript

- We use the file extensions as `.js` or `.cjs` for Common JavaScript.
- We can use `require` and `module.exports` in Common JavaScript.

## Module JavaScript

- We use the file extension as `.mjs` for Module JavaScript.
- We can use `import` and `export` in Module JavaScript.
