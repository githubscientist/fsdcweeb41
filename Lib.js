function add(x, y) {
    console.log(`the sum of ${x} and ${y} is ${x + y}`);
}

function sub(x, y) {
    console.log(`the difference of ${x} and ${y} is ${x - y}`);
}

module.exports = {
    add,
    sub
}